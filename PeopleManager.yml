# Database settings
database:
  # the name of your JDBC driver
  driverClass: org.sqlite.JDBC

  # the JDBC URL
  url: jdbc:sqlite:PeopleManager.db

  # any properties specific to your JDBC driver
  properties:
    hibernate.dialect: org.hibernate.dialect.SQLiteDialect